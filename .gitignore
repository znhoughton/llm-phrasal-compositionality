.Rproj.user
.Rhistory
.RData
.Ruserdata
*.wav
*.mp3
*.rds
*.RDS
c4_10B_2B_local/
hf_cache/
model_cache/
# Track the paper source, the rendered PDF and the script that builds its results;
# ignore everything else under paper/ (figures, logs, the 54 generated results CSVs,
# the submission zip). "paper/*" rather than "paper/": git does not descend into an
# ignored directory, so a plain "paper/" cannot be negated per file.
paper/*
!paper/.gitignore
!paper/writeup.qmd
!paper/writeup.pdf
!paper/prepare_results.R
!paper/references.bib
**/__pycache__/
*.pyc
.ipynb_checkpoints/
**/.ipynb_checkpoints/
Analyses/analysis-script.html
**/all_layers_results.csv
# Whisper's are small enough (~13MB) to track normally, unlike OLMo/BabyLM's
!Data/whisper/**/all_layers_results.csv
!Data/whisper_subword/**/all_layers_results.csv

# Pipeline backups of the pre-filter results (~327MB each, regenerable).
*.pre-filter-bak

# Run transcripts from the analysis scripts above. The scripts are tracked; their output
# logs are regenerable and timestamped, so they only add churn.
Analyses/*.log
model_refit_*.log

# Local staging copies left by the data pipeline, alongside the *.pre-filter-bak rule.
*.stale-local-bak

# The posted arXiv build, unpacked locally to diff the paper against it. paper/writeup.qmd
# is the source of truth and regenerates the same PDF, so this is a local reference only.
arxiv_v2/

# Per-layer classifier outputs and their diagnostic plots. Nothing reads these back:
# paper/prepare_results.R reads the all_layers_results.csv aggregate in the same directory,
# which carries identical columns and is simply their concatenation. Keeping both tracked
# duplicated ~2.7GB for no benefit.
**/layer_[0-9][0-9].csv
**/layer_[0-9][0-9]_plot.png

# ...but do track the prepared results the paper is actually built from (~10MB), so the
# figures and tables can be rebuilt from a clone without the raw data or a GPU.
!paper/results/
